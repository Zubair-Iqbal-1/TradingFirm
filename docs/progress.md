# Progress

One row per part from `docs/plan-analyst-watcher.md`. Updated at the end of every part, before the commit.

## Next: 4.5 (journal scoring), then 4.7, then 4.6

- **Order: 4.5 → 4.7 → 4.6.** 4.4 is closed (CI run #15).
- **Base:** the commit that adds this block (`docs: CI run #15 closes 4.4; next-part block`); check that `git log origin/main -1` shows it. Suites at run #15: data-engine 504, risk-shield 857, ai-agent 629. All green, 0 skipped.
- **Read for 4.5:** plan row 4.5, spec 4.4 (`ai.verdicts`, `ai.verdict_outcomes`), `docs/decisions.md` from 2026-09-21. Two real AAPL verdicts are in `ai.verdicts` to score.
- **The earnings-hold decision joins 4.7.** Code summarizes the 8 stored earnings reactions; the model decides hold / exit-before and cites them; a worst stored move larger than the stop distance forces exit-before in code, whatever the model says.
- **4.5 is built; its deploy waits for a go** (row below; AAPL scores at +1 09-22, +5 09-28, +20 10-19, +30 11-02, +60 12-15): migration 008 first, then the ai-agent build + up outside 17:25–18:15 ET. **`d82c16e` rides that rebuild**, named in the deploy report and inside the green CI run. From then on ai-agent has a scheduled writer (the 17:30 ET journal slot).
- **Open for 4.8:**
  - **Reasoning tokens were 0 at effort `low`** on all three live calls. Compare `low` against `medium` on the five tickers: verdict quality, tokens, cost.
  - **The five-ticker run** with `./scripts/analyze_live.sh`, about $0.058 a ticker on a first analyze.
- **Open items (none are 4.5's):**
  - ATH gap: `no_target` when there's no zone above entry. 4.4 passes it through as a `wait` reason.
  - Market (`_MARKET`) headlines are still unlabelled: nothing calls the classifier automatically. 4.6 decides who does; at $0.039 a full batch, a loop there re-opens the 40 cap.
  - Contract-aware futures, due before the December expiry.
  - CI runner pin to `ubuntu-24.04` before 2026-10-19 (the notice is on run #15 too), in the housekeeping batch.

| Part | Status | Commit | Date | Notes |
|---|---|---|---|---|
| 0.1 | done | 3ed39cf | 2026-09-04 | `.env.example` + real `.env` created; `docker-compose up -d` boots all 7 containers; `curl :8001/health` → `db_connected: true`. Blocker along the way: host `docker.sock` belonged to a different macOS user account and was unreachable until the user restarted Docker Desktop under this account. |
| 0.2 | done | 05128f5, 4ac7c34 | 2026-09-04 | Migrations mounted read-only into postgres's `/docker-entrypoint-initdb.d/`, so a fresh `pgdata` volume applies `001_initial_schema.sql`; `scripts/migrate.sh` handles existing volumes by tracking applied files in `public.schema_migrations` (rerun is a no-op). It runs entirely through `docker exec` into `tf-postgres`: no host `psql`, no `.env` parsing, no `mapfile` (macOS bash 3.2). Verified by running it from the host shell; `\dn` shows the six schemas. |
| 0.3 | done | ee0e131 | 2026-09-04 | `HEALTHCHECK` in all four FastAPI Dockerfiles (`python3 -c urllib...` against `/health`) and `web/Dockerfile` (busybox `wget --spider`); `web` now waits on `data-engine` with `condition: service_healthy`. Healthchecks target `127.0.0.1`, not `localhost`: inside the container `localhost` resolves to `::1` first and Next's standalone server binds IPv4 only, which kept `tf-web` unhealthy. Verified: `docker compose up -d --build`, all 7 containers `(healthy)`. |
| 0.4 | done | 71b21f8 | 2026-09-05 | Removed the legacy "Scanner 1" tab, its API routes (`web/app/api/scanner/{run,discover,filter,technical}`), `web/lib/scanner/*`, `web/lib/data/*`, `StockCard.js`, the `firebase` + `yahoo-finance2` deps and the Google sign-in UI (D18 parks auth); `scanner/README.md` marks the Python reference frozen. Verified: the user ran `npm install` / `npm run build` (auto mode blocked them in-session), `tf-web` rebuilt, `/api/scanner/run` 404s and `/api/scanner/pro` still proxies to a healthy `data-engine`. Run Scanner was not clicked (G6). |
| 0.6 | done | e05c746 | 2026-09-05 | Added `docs/runbook.md`: Energy Saver settings (prevent sleep on power adapter, wake for network), `caffeinate -dims` as a belt-and-braces layer, Docker Desktop auto-start (start on login + restore last containers), and a numbered post-Wi-Fi-drop checklist (containers healthy → data-engine `/health` → in-flight scan status → watcher heartbeat once Phase 6 exists → notifier `/status` once Phase 5 exists). No test (plan marks this part n/a); no code changed. |
| 0.5 | done | 7bf96d1 | 2026-09-05 | `providers/fixture_provider.py` (`FixtureProvider`, reads `tests/fixtures/{daily,hourly,info}/<TICKER>.json`, no network), `tests/record_fixture_live.py` (manual live recorder via `YFinanceProvider`, `to_json(orient="table")` for lossless round-trips; JSON over parquet to avoid `pyarrow`), `tests/test_fixture_provider.py` (7 tests). Recorded AAPL as a canary, then AAPL/MSFT/SPY (252 daily + 448 hourly rows each, one bulk call per interval, 2 s between `Ticker.info` calls). Verified 14/14 inside `tf-data-engine`: the host's pandas 3.0.3 silently changes `read_json` datetime resolution vs the pinned 2.3.0, which is why tests run in the container from here on. `assert_frame_equal(check_freq=False)` because JSON drops `index.freq`, as real yfinance data does. |
| 0.5 follow-up | done | ad479ba | 2026-09-05 | `get_provider()` accepts `fixture` (`tests/test_provider_factory.py`, 2 tests) so `DATA_PROVIDER=fixture` selects the offline provider; `requirements-dev.txt` pins `pytest==9.1.1` + `pytest-asyncio==1.4.0`, kept out of the prod image. `CLAUDE.md` and `docs/overview.md` updated. Verified 16/16 in the dev container, prod build restored, `/health` green. |
| 0.4 follow-up | done | 1d1203e | 2026-09-05 | Hoisted `mapProResults` in `web/app/page.js` to module scope (the one pre-existing `react-hooks/immutability` lint error); `npm run lint` clean, `npm run build` green, no behaviour change so `tf-web` was not rebuilt. `docker-compose.yml` `LLM_PROVIDER` default `gemini` → `anthropic` per D16; verified via `docker compose config`. |
| 1.1 | done | 717ecbf | 2026-09-05 | Migration `002_bars.sql`: `data_engine.ohlcv_bars` (PK `(ticker, interval, ts)` plus a `ts DESC` index), `IF NOT EXISTS` only. `db.upsert_bars()` (`ON CONFLICT` update, no-op on empty) and `db.get_bars(since=None)` (ascending, optional `ts >=`); `tests/test_bars_store.py` (4 tests) asserts the literal SQL and parameters against a mocked asyncpg pool. Verified 20/20 in `tf-data-engine`; `./scripts/migrate.sh` applied 002 to the live Postgres, rerun a no-op; prod build restored. |
| 1.2 | done | bf542e2 | 2026-09-05 | `POST /stock/{ticker}/refresh`: daily (2y) + hourly (3mo) via `app.state.provider`, upserted through `db.upsert_bars()`, frames `del`'d + `gc.collect()`; returns `{ticker, dailyBars, hourlyBars}`. 15-minute cooldown per normalized ticker (`tf:cache:refresh:{ticker}`, 429 + `Retry-After`), written only after a successful upsert, with an `InMemoryStore` fallback so Redis-down does not fail open; `db_pool` absent → 503. `tests/test_refresh_endpoint.py` (6 tests) calls the endpoint function directly with an in-process fake Redis so the real `cache.py` path runs; 26/26 in `tf-data-engine`, prod rebuilt, `/health` green. No live yfinance refresh was run (G6). |
| 1.3 | done | 309a955 | 2026-09-05 | `MarketScanner(provider, db_pool)` persists daily + hourly bars for final scan winners via `db.upsert_bars()`, capturing a ticker's frames only when it survives a stage and freeing them with the bulk cleanup (G8); a missing pool or a failed upsert is logged and skipped. Fixed a real pre-existing bug: Finviz candidates were never case-normalized, so `main.normalize_ticker()` is now the one shared normalizer (G1.5) and `bar_records_from_df()` moved into `db.py` for both write paths. `tests/test_scanner_pipeline.py` +5 (`TestPipelinePersistsWinnerBars`); 31/31 in `tf-data-engine`, prod rebuilt. Deferred: `upsert_bars()` has no explicit transaction, so a mid-`executemany` raise can leave one ticker partially written. |
| 1.4 | done | 1e61025 | 2026-09-06 | `GET /stock/{ticker}/bars?interval=1d\|1h&since=`, DB only, never the provider; new `db.bar_record_to_json()`. Two-query shape on purpose: an unfiltered `get_bars()` decides existence (404), a second `get_bars(since=)` supplies the filtered result (200 with `[]` when everything is filtered out); naive `since` is treated as UTC; a raise mid-query is 503. `tests/test_bars_endpoint.py` (11 tests); 42/42 in `tf-data-engine`, prod rebuilt. |
| 1.5 | done | d7bb455 | 2026-09-06 | Split `indicators/technical.py` into `moving_averages.py`, `volatility.py` (+`extension`, `gap`), `momentum.py` (+`rsi`, `macd`, `relative_strength`), `volume.py` (+`avg_dollar_volume`); `indicators/__init__.py` re-exports all 12 names and is the only import surface (both importers switched). Behaviour change recorded in decisions: `calc_atrp` / `check_52w_position` return NaN on empty input instead of raising, `calc_rvol` keeps `0.0`, divisions never return inf, `rsi` is SMA-seeded Wilder, `macd` keeps reference parity with no warm-up mask. `tests/test_indicators.py`: 33 authored / 45 collected, every hand-computed value passed first run; 87/87 in `tf-data-engine`, prod rebuilt. Deferred: `rsi()` is a per-bar Python loop, vectorize only if intraday bars reach it. |
| 1.6 | done | 7ee44bc | 2026-09-06 | New `indicators/levels.py`: `fractal_swings` (strict, wing 2, NaN disqualifies the window), `volume_nodes` (50 close bins, top 5), `merge_levels` (0.5 % of the running mean), `score_zones`, `support_resistance` (top 3 per side around the last valid close, `Zone` dataclass); six names added to `indicators.__all__` (18); conventions in `docs/decisions.md` (zones entry). `tests/test_levels.py`: 19 authored / 23 collected on a 17-bar series with hand-derived pivots, all passed first run; 110/110 in `tf-data-engine`; AAPL fixture sanity gives support 273.51–275.95 (75) and resistance 334.70 / 344.27 (0). Deferred: `fractal_swings` is a per-bar loop, same note as `rsi()`. Tests still displaced the prod container via the dev overlay, which led to Part 0.7. |
| 1.6 follow-up | done | 21dffdb | 2026-09-06 | Volume is no longer a zone "method": `methods` ⊆ `{swing_high, swing_low}`, `Zone.volume_node` added, volume still +25 but the +30 bonus needs both swing types; rubric pinned 0 / 25 / 25 / 50 / 90 (superseding decisions entry). AAPL sanity: 254.49–255.01 drops from 75 to 45 and out of the top 3, 286.71–287.84 (50) takes its place. Counts unchanged, 110/110, prod rebuilt. |
| 0.7 | done | 12ef8f1, 627c71c | 2026-09-06 | Added via `docs/decisions.md` (plan is read-only). Compose service `data-engine-dev` (`tf-data-engine-dev`, profile `dev`, port 8011, source volume-mounted, no `depends_on`) from a new Dockerfile `dev` stage (base + `requirements-dev.txt`, no `COPY . .`; `prod` stage is last and named by `data-engine`), isolated by construction: `DATA_PROVIDER=fixture`, database `tradingfirm_dev`, Redis DB 1. `scripts/dev-db.sh` creates that database, applies migrations through a new `MIGRATE_DB` override in `migrate.sh`, and restarts the dev container so the API picks the database up (`627c71c`); `pytest.ini` locks discovery to `tests/test_*.py`; rule G13 added. Verified: prod image layers 1–8 byte-identical before/after the Dockerfile split with only `Dockerfile` + `pytest.ini` differing in `/app`; 110/110 in the dev container with prod's container ID unchanged; `dev-db.sh` idempotent; prod `ohlcv_bars` and `schema_migrations` untouched. The first spec draft missed that a dev API carrying prod's `DATABASE_URL` / `REDIS_URL` would write fixture bars into prod tables, hence the separate database and Redis index. |
| 1.7 | done | 26c7ac4 | 2026-09-06 | `GET /indicators/{ticker}`: plan §3 swing set + 1.6 zones from stored daily bars, RS vs SPY and the sector ETF from the same bar store, Redis cache 15 min with `cached` set on the way out (not stored) and dropped by refresh. New `indicators/snapshot.py` (`swing_snapshot`, pure), `sectors.py`, `models.py`, `db.get_stock()` / `db.bars_to_df()`, shared `tests/fake_redis.py`; conventions in `docs/decisions.md` (endpoint entry). Verified: 34 authored / 45 collected new tests (19 endpoint, 22 indicators incl. the 12-way sector map, 4 store), 155/155 in `tf-data-engine-dev`; live on 8011 after refreshing the AAPL + SPY fixtures the second call is `cached: true` with TTL 900 in Redis DB 1 and the zones equal the 1.6 sanity numbers; prod rebuilt, the new route answers 404 there (no bars). Deviations from the approved spec: endpoint tests drive a mock pool through the real `get_bars` / `get_stock` SQL instead of patching; the response model is exported from `indicators` (`__all__` = 22); added `close`, `asOf`, `sector` and `benchmarks.{spy,sector}.{ticker,bars}`. Deferred: the refresh cooldown key logic (Redis check + `InMemoryStore` fallback) still lives inline in `main.py`'s refresh endpoint next to the new cache invalidation; move it into `cache.py` as its own refactor, not inside a feature part. |
| 2.1 | done | dc739ed, 8e90f38 | 2026-09-09 | `providers/context/finnhub_client.py` (key in header, 60/min + 1.2 s limiter with injectable clock, typed errors, no retries) and `finnhub.py` (five fetchers returning raw bodies cached via `cache.py`, pure row converters, `sync_context()` storing news + earnings events); migration `003_context.sql` (`news_items` with `ticker` NOT NULL and `_MARKET` sentinel, `events` with merged `meta`); `db.upsert_news` / `upsert_events`; `FINNHUB_API_KEY` wired to prod only, dev twin hard-coded empty; `respx==0.22.0` in dev deps; migrations mounted read-only into the dev twin. Preceded by `dc739ed` (`normalize_ticker` → `tickers.py`). Verified: §18 live check with 5 calls for AAPL, all five endpoints free-tier reachable; fixtures recorded; 36 authored / 40 collected new tests, 195/195 in `tf-data-engine-dev`; 003 applied to `tradingfirm_dev` and prod (`schema_migrations` lists it); live psql proves `_MARKET` dedup and `meta` merge; dev twin raises `FinnhubNotConfigured` with zero HTTP. Deviations and findings: calendar and surprise events are two rows (Finnhub keys surprises by fiscal period end), and the free calendar returns only the upcoming report, so 2.3 needs another source for past report dates; `sentiment` stays NULL until Phase 4. Incident: the compose config render printed the real key into the session transcript (my masking assumed quotes); the user was told to rotate it; key rotated 2026-09-09. Follow-up docs commit before 2.2: `AAPL_news.json` trimmed 246 → 20 items (recorder now caps news at 20), rule G14 tightened, prod-migration rule added, `dc739ed` decisions entry names the scanner as the fifth caller; the 0.5 recorder was checked and has no `sys.path` bug (it carries the same insert line the 2.1 recorder copied). |
| 2.2 | done | 7656801 | 2026-09-09 | Spec `docs/specs/2.2.md` (approved after review; built first without one, which reworded G1). `providers/context/edgar_client.py` (declared `User-Agent` from `EDGAR_USER_AGENT`; one process-wide limiter `ratelimit.edgar_limiter`, rolling 10/s; 403/429 → `EdgarRateLimited`, no retries) and `edgar.py` (`cik_map` cached 24 h; `recent_filings(ticker, forms, days)` → `(rows, truncated)`, cache = parsed rows within 90 days + the block's oldest date, 15 min; unequal `recent` columns fail closed; not-in-map and 404 fail open; `sync_filings()`); migration `004_filings.sql` (`data_engine.filings`, PK `(ticker, accession)`, `filed_on DATE` = official filing date, `accepted_at` nullable, `ON CONFLICT DO NOTHING`); `db.upsert_filings`; `EDGAR_USER_AGENT` wired to prod compose only, dev twin hard-coded empty, `.env.example` blanked. Verified: §18 live check, 2 calls for AAPL, both 200, `acceptanceDateTime` confirmed UTC; fixtures recorded (5 tickers / 60 filings); 48 authored / 68 collected new tests, 263/263 in `tf-data-engine-dev`; edited 004 rebuilt on `tradingfirm_dev` (old table + `schema_migrations` row dropped, `dev-db.sh` re-applied); live proof on the dev DB that a second insert on the same accession is ignored and a NULL `accepted_at` stores; dev twin raises `EdgarNotConfigured` with zero HTTP. Deferred (decisions 2026-09-09): class shares unreachable through both fetchers (future `refactor:`), 2.1 wrong-shaped-cache gap, Finnhub limiter per-client, one uvicorn worker assumed; a required `recent` column (`form`, `filingDate`, `accessionNumber`) absent altogether currently makes every row incomplete and returns `[]` with only a log line — should fail closed like unequal columns (`EdgarError`, nothing cached; optional columns absent stay None), fix with its own decisions entry the next time the parser is open. G15 done on go 2026-09-09: `./scripts/migrate.sh` (004 on prod, `schema_migrations` 001–004) and `docker compose up -d data-engine` (env var picked up, container recreated, no `--build`: the prod image still predates 2.2, inert until 2.4). **Carried to 2.4's G15 list: `docker compose up -d --build data-engine` on prod when the dossier endpoint ships; no rebuild before then.** |
| 2.3 | done | 7b7aa16, 72295f3 | 2026-09-09 | Spec `docs/specs/2.3.md` (approved after a v2 rewrite; the plan row assumes stored earnings dates, which 2.1 could not supply). Two commits. **Dates:** `DataProvider.get_earnings_dates()` (yfinance `limit=12`, `YFRateLimitError` → new `ProviderRateLimited`) primary, `providers/context/alphavantage_client.py` + `earnings.py` fallback, called only when the primary yields no usable past date — never on a rate limit, never when the bar store is empty. Report dates validated against the store (a bar date or ±1 day), never the refresh frame: refresh downloads 2 y while the feed reaches 6 back. `meta.earnings.{source, validated, hour, epsEstimate, epsReported, surprisePct}` merges beside 2.1's `meta.calendar`; refresh always answers `earningsDates: {source, stored, dropped, reason}` and a failure there never fails a refresh whose bars were stored. **Reactions:** pure `indicators.earnings_reactions` + generic `db.get_events` + `earnings_reaction_history`; unknown hour and cross-source date conflicts both settled by `calc_rvol >= 2`, never by the bigger move. Live check: 4 calls, all clean — columns `EPS Estimate` / `Reported EPS` / `Surprise(%)`, tz-aware Eastern index, `limit=12` actually returns **25 rows** over six years, SPY → `None` (recorded as `null`), Alpha Vantage values are strings with `"None"` as the null sentinel and `reportTime` present. Verified: 87 authored / 95 collected new tests, 358/358 in `tf-data-engine-dev`. Deviations from the approved spec, both recorded in `docs/decisions.md`: rows older than the stored bar history count as `out_of_range`, not `dropped` (20 of AAPL's 25 rows land there, which would have made `dataQuality` meaningless), and an unvalidated *future* row never counts as dropped; the hour also falls back to `meta.calendar.hour` so 2.1 rows still produce reactions. Three pre-existing assertions updated for the new response field and the 23rd indicator export. G14: the Alpha Vantage key rides in the `apikey` query parameter (no header form exists) under two conditions in that client module — `httpx` logger pinned to WARNING, typed errors raised `from None` — asserted by a caplog test over a 4xx and a transport error. Deferred: no Redis cache on the read side (2.4 caches the dossier); the scan pipeline does not fetch earnings dates (one `Ticker` call per winner); the Alpha Vantage 5-per-60 s window is proven by two tests together, not one end to end — `test_alphavantage_limiter_caps_rate` exercises a locally built limiter with a fake clock and `test_alphavantage_default_limiter_is_five_per_minute` asserts the shipped client uses the module-level instance with those numbers, so a change to how that shared instance is constructed is caught only by the second (accepted as is, no test to add). **G15: no prod action. The recreate for `ALPHAVANTAGE_API_KEY` was skipped by decision 2026-09-09 — the running prod image predates 2.2 and does not read the variable, so recreating now would be downtime for nothing. Carried to 2.4's `docker compose up -d --build data-engine`, which picks up `EDGAR_USER_AGENT` (carried from 2.2) and `ALPHAVANTAGE_API_KEY` in one restart.** |
| 2.4 | done | 400549d, bb98288, c2b7f78 | 2026-09-09 | `GET /dossier/{ticker}?horizon=swing` assembles all of Phase 2 into one document; every section carries a `status`, an upstream failure degrades one section and a DB failure is a 503 for the whole document. Three commits: refactor (cooldowns + the indicator body + `cached_json(ttl_for=)` out of `main.py`), assembly, endpoint. Two things the spec had wrong, both caught by a failing test: `DB_ERRORS` cannot contain `OSError` (`asyncio.TimeoutError` is a `TimeoutError`, which subclasses it, so every timed-out section read as a dead database), and `sync_context` cannot serve the dossier (it writes only after all three fetches succeed, so a failed calendar call would drop the news rows too) — each section now owns its calls and its write. 358 → 408 tests. Spec corrected to what shipped (three `Correction made during commit N` notes), each with a `docs/decisions.md` bullet. Prod rebuilt on the go (`c85b437e0acd` → `01514c9896ff`), all three keys present, `/health` green. The 2.5 live check then found two counting bugs, fixed under this spec (`Correction made after 2.5`): the budget counted per section over a shared concurrent client (14 reported for 10 made), and `budget` was inside the cached body so a hit replayed it. 408 → 409 tests. **Deferred:** `calls_made` is per client object, so two dossiers assembled at the same instant cross-count each other — a `contextvars` counter inside the clients is the fix if more than one caller uses `/dossier`. |
| 2.5 | done | 3fbda88 | 2026-09-09 | `tests/dossier_live.py`: manual, one ticker, wraps each client with a timer and drives `main.get_dossier` in-process — prints every call with its wall clock, every section's status, the budget, and a second request that must be `cached: true` with zero upstream calls. First run against prod: 10 calls (5 Finnhub, 2 EDGAR, 3 yfinance), 9.73 s, slowest source 4.18 s (`/stock/profile2`; EDGAR was the fastest at 0.48 + 0.20 s, nothing near the 8 s bound), every section `ok` except `news` `truncated` at the 30-headline cap out of 245 stored. It found the two budget bugs fixed in `10c789b`. Re-run after that fix read **0**, not 2, because the dossier document itself was still cached (written 14:32 ET, market open, TTL 900 s) — the cached body now correctly reports `upstreamCalls: 0, bySource: {}` where it previously replayed 14. After the key expired, a plain `curl :8001/dossier/AAPL?horizon=swing` against the serving image gave the warm path: **`upstreamCalls: 2` (`edgar: 1` submissions + `finnhub: 1` news, the two 15-min keys), 435 ms, `bars ok` / `refreshed: false`**, then `cached: true` with 0. Prod image `01514c9896ff` → `41c9d163d8ee`, `/health` green. Phase 2 closed. |
| 3.1 | done | 22b7d67, d67f51c, f6a5625 | 2026-09-09 | Spec `docs/specs/3.1.md` (approved after a v2 review; two corrections applied inline during the build and recorded in the spec). `services/risk-shield/{config,db,cache}.py` + a fail-open lifespan; migration `005_risk.sql` (`risk.macro_briefs`, own `CREATE SCHEMA`, no `user_id`, `health_checks`' regime CHECK reused); dev twin `risk-shield-dev` on 8013 (`tradingfirm_dev`, Redis DB 1, empty FRED key, `/migrations` mounted read-only), Dockerfile split base/dev/prod, pytest pins identical to data-engine's. **Not a mirror of data-engine in three places, all in `docs/decisions.md`:** startup is *bounded* (asyncpg defaults to a 60 s connect timeout, redis-py to none, so a restarting Postgres would stall boot for a minute — each dependency now gets one `asyncio.wait_for(config.STARTUP_TIMEOUT)` around the factory *and* its verification call, ~10 s worst case); `config.STARTUP_TIMEOUT` is read at call time so the slow-path tests can patch it (the whole suite runs in 0.36 s); `FRED_API_KEY` is a `SecretStr`. `/health` reports boot state, not a live probe — the same contract data-engine has, now written down as a limitation instead of implied. Verified: 44 authored / 44 collected, all offline, in `tf-risk-shield-dev`; `005` applied to `tradingfirm_dev` and the rerun a no-op, plus a direct re-execution of the file to prove `IF NOT EXISTS` rather than only the tracking table; `curl :8013/health` → `db_connected: true, redis_connected: true, fredConfigured: false`. Three feat commits under the one spec (config+db+twin / cache / lifespan+migration): code + tests came to 1,177 lines against a ~450 estimate, and a two-way split would still have left one commit at 760. One test failed first run — `test_migration_005_creates_macro_briefs` matched `user_id` inside the comment explaining why there is no `user_id` column; the assertion now reads statements with `--` lines stripped. **Deferred:** a probing `/health` (with the reconnect logic it implies) for both services; bounding data-engine's startup the same way, as its own `refactor:`. **G15 done on go 2026-09-10:** `./scripts/migrate.sh` (005 on prod, `schema_migrations` 001–005, `risk.macro_briefs` present) and `docker compose up -d --build risk-shield` (image `dfbab6c2ed93`, rebuilt from `09e40c2` to the same ID; `:8003/health` → `db_connected`, `redis_connected`, `fredConfigured` all true; `tf-data-engine` still `41c9d163d8ee`). The container was recreated at 10:09 CEST by the operator's own `up` before the migrate — operator order, not a process gap; harmless since 3.1 reads no table. **Carried to 3.2:** see `docs/decisions.md` 2026-09-10 (Part 3.1 → 3.2 carry-forward). |
| 3.2 | done | 10bcf0b, 5a5b307, 91d5c94 | 2026-09-10 | Spec `docs/specs/3.2.md` (approved on v2). `monitors/quotes.py`: one yfinance download of the 17 core tickers, cached 5 min (120 s when degraded). `monitors/fred.py` + `fred_client.py`: 8 series, cached 6 h (120 s when empty), key in the query string on the Alpha Vantage conditions. Source cooldowns copied from data-engine; `ratelimit.py`; `cached_json` refuses `None` and regains `ttl_for`. Verified: 64 authored / 78 collected new tests, 122/122 in `tf-risk-shield-dev`; all 63 spec-named tests exist. Live canaries, isolated container, all clean: FRED 1 then 8 series (9 requests; `"."` is the missing marker); yfinance SPY then the 17 (4.46 s cold, no rate-limit record). Findings: 1.5.1 swallows `YFRateLimitError` (hence log capture); a cold download is 34 requests; per-ticker dates differ (`^VIX` and futures carry today's bar); `^VIX` volume is 0. Two spec corrections: memory-fallback FRED cooldown is 900 s; the lock is per event loop. **G15: no prod action.** The prod rebuild for the new pins and the 3.3/3.4 carry-forward are in `docs/decisions.md` 2026-09-10. |
| 3.3 | done | 6077699, 8d03fa6, bcde3ee, 2e6b51a, 6f57846 | 2026-09-10 | Spec `docs/specs/3.3.md` (approved v2). Six pure monitors in `monitors/regime.py` return `{score, raw, detail, stale}`. Tickers pair only through `series.align()` on date; a same-day bar downloaded before 16:15 ET is partial and only `vix` reads it. `get_quotes_view` serves a 24 h last-known body (`tf:risk:cache:quotes_last`) with `stale: true` on a cooldown, refusal, error or degraded answer. `scoring/health_calculator.py` computes integer-weighted, half-up scores with a coverage floor of 70; `regime_classifier.py` maps them to the four regimes. `compute_health()` exists, with no caller yet. Breadth is RSP/SPY only (no A/D source). Verified: 58 authored / 152 collected new tests, 274/274 in `tf-risk-shield-dev`; the `compute_health` tests run the real monitors on a real view. Code 715 vs ~560, tests 915 vs ~870. Commit 1 hit 640 lines and was split before push; commit 2 was split from the start. Provisional numbers are in `docs/decisions.md`. **Deferred:** FRED last-known key, `FredCoolingDown` → stale, and cadence-based freshness → 3.6 (first FRED reader). Early-close days count as partial until 16:15 ET, so `exchange_calendars` early-close handling → 3.4. **G15: no prod action**; `docker compose up -d --build risk-shield` stays on 3.4's list. |
| 3.4 | done | 7738779, b10257d, 5647ca2, b5324a7, 608c157, ca7f2f2, 8986feb | 2026-09-10 | Spec `docs/specs/3.4.md` (approved v2, five corrections). **Scheduler:** XNYS-gated (`exchange_calendars` 4.13.2), every 5 min from open to close plus a 16:20 ET settle check, off unless `SCHEDULER_ENABLED`, `--workers 1` pinned. **Each check:** writes `risk.health_checks` and publishes `tf:risk:health` under Part 5's throttle. **Endpoints:** `GET /market/{health,indicators,history}`, Postgres only. **Night mode:** split to 3.4b (`docs/decisions.md`, supersedes the plan row's night cadence). **Verified:** 63 authored / 79 collected new tests, 353/353 in `tf-risk-shield-dev`. Dev-DB round trip: 404 `no health checks yet` before any row; two synthetic checks gave `initial` then `regime_change` on `tf:risk:dev:health` only, trend `improving`, `jsonb` / `timestamptz` rows, all three routes served. **Corrections during build:** the channel tests pinned their env; commit 3a (610 lines) split before push. **Estimate:** code 809 vs ~450, tests 1,273 vs ~850. **G15 done on go 2026-09-10:**
- **Before:** the dev round-trip rows and the Redis DB 1 state key were deleted.
- **Deploy:** `docker compose up -d --build risk-shield`, image `dfbab6c2ed93` → `b3ab1134a974` (`rollback-3.3` keeps the old one); one uvicorn worker.
- **First check:** 16:50:05 UTC, about 5 s cold, CAUTIOUS 68, all six monitors fresh, published `initial`, one row. The 12:45 ET slot was skipped at boot by the grace rule. |
| 3.5 | done | 6142a04, 71047a4, 756e360, 7ccd50d, 9251f34, 9604c2d, 8a775ba, 4fdd006, ad17cfb | 2026-09-10 | Spec `docs/specs/3.5.md` (approved v2 + additions 8–9 + option A after the live check). **Calendar:** `data/econ_calendar.json` (16 FOMC / CPI / jobs events, Q3–Q4 2026) + `GET /market/calendar`; renew by 2026-12-17. **News:** risk-shield polls Finnhub general news every 15 min (prod only) → data-engine `POST /news/ingest` → `_MARKET`. **Live check:** a 100-item page spanning ~41 h, ids in pickup order, so no `minId`. **Safeguards:** pinned limit copies on both sides; reads data-engine's Finnhub cooldown; `newsPollStale` on `/market/health` and the payload. **Verified:** risk-shield 353 → 448, data-engine 409 → 435, in the twins. Twin-to-twin round trip: `_MARKET` 0 → 21 → 21, the oversize item stored as title 1,000 / summary 10,000 / source 100. Both prod-stage images inspected offline. **Estimate:** code 1,119 vs ~770–1,060 (the 141-line live script counted), tests 1,509 vs ~1,550–2,140. 4c measured 529 vs 208–288: addition 8 landed after the bands were set, and its band was never re-cut (rule in decisions). **G15 done on go 2026-09-10** (settle timing waived: nothing reads the settle yet):
- **Before:**
  - the 21 round-trip rows deleted from `tradingfirm_dev`
  - rollback tags `tradingfirm-data-engine:rollback-3.4` = `41c9d163d8ee` and `tradingfirm-risk-shield:rollback-3.4` = `b3ab1134a974`
- **Deploy:**
  - `docker compose up -d --build data-engine` at 18:57:44 UTC: `41c9d163d8ee` → `65ec3104f32c`, `/news/ingest` in `/openapi.json`
  - `docker compose up -d --build risk-shield`, booted 18:58:12: `b3ab1134a974` → `72b8099801a1`, one uvicorn, `/health` all true, calendar 16 events
- **First polls:**
  - 19:00:00 UTC `100 fetched, 100 sent, 0 dropped, 0 truncated, page span 2444 min`; prod `_MARKET` 0 → 100
  - 19:15:00 `100 fetched, 100 sent, page span 2409 min`; 100 → 103 (97 duplicates absorbed); no overlap warning; `newsPollStale: false`
- **Slots:** the rebuild window held no slot boundary, and 19:00 ran on the new image. 18:45 and 18:55 UTC have no `health_checks` row. Both fell before the rebuild, on the old image, whose logs went with the recreate, so the cause is unknown. |
| 3.6a | done | b3c1cce, cfcb681, 88cd366, 4106dae, 1740c5d, 1768eb8, 85fb611, 25f8079 | 2026-09-10 | Spec `docs/specs/3.6a.md` (v2 + amendments A–C). Plan row 3.6 split into 3.6a / 3.6b (`452062d`, decisions). **Built:**
- migration 006 (`macro_briefs.brief`, `trigger`)
- data-engine `GET /news/market`
- FRED last-known (7 d), refusals as stale, freshness by series cadence
- `macro_inputs.assemble_inputs` + `GET /macro/brief/inputs` (60 s reuse)
- `MACRO_BRIEF_ENABLED` / `AI_AGENT_URL`, off in prod, twin hard-coded off / `.invalid`

**Step 0:** 8 FRED requests (one per series), all 200; the cadence table stood. **Verified:** risk-shield 448 → 546, data-engine 435 → 452 in the twins. Twin round trip: health `ok`, settle present, 20 news items via `:8011`, calendar `ok`, FRED `no_data`, `anyStale: true`, second call `cached: true`; dev rows deleted. Pre-part slot check: no gaps on `72b8099801a1` 19:00–20:20 UTC. **Estimate:** code 678 vs 663–969, tests 1,263 vs 1,244–1,778; 4a re-cut to its measured 369. **Outside 3.6a:** `2325d49` froze the clock in `test_dossier_camelcase_shape` (a hard-coded 09-09 broke at the 09-10 close). **G15 done 2026-09-10** (recorded 2026-09-11): 006 on prod (`schema_migrations` 001–006); data-engine `b829dfb8e97c` and risk-shield `de71ecbae2b2`, both up ~21:07 UTC. |
| 3.4 follow-up | done | 84c7453, 783f21d, 8f0d576, c5c10b3 | 2026-09-11 | Spec `docs/specs/3.4-follow-up.md` (approved + additions 1–2). **Built:**
- `wallclock.py`: sleeps of ≤ 60 s, the wall clock re-read after each, a `host paused` WARNING when wall beats process time by > 120 s
- both loops wait through it; the scheduler also warns for slots a wake between slots passed
- `pausedSeconds` for the first check after a pause: appended to the health payload, and in every row's `indicators`

**Verified:** risk-shield 546 → 562 in the twin (16 new tests, 10 updated in place). A real 130 s wait in the twin slept 60.06 / 60.03 / 9.93 s. No direct `sleep(` outside `wallclock.py` and `ratelimit.py`. **Correction during commit 3:** one expected pause line assumed a 60 s process clock; loop tests use the real one (`~2h 1m`). **Estimate:** code 128 vs 119–173; tests 344 vs 364–518 (commits 2–4 under their test floors). **G15 done on go 2026-09-11, in session** (exception in decisions):
- **Before:** `rollback-3.6a` = `de71ecbae2b2`; logs saved; `docker compose build risk-shield` → `1cfb238d7a7b`.
- **Deploy:** `up -d --no-build` at 17:46:01 UTC, 61 s after the 17:45 slot started (its row landed at 17:45:02 on the old image). `/health` 200 after 4 s, both "running" lines, no ERROR.
- **After:** 17:30–17:55 one row per slot, none missed or doubled; 17:50 / 17:55 / 18:00 checks on the new image with the `pausedSeconds` key; news poll 18:00 `100 fetched, 100 sent`. The boot logged the expected `Missed 1 … up to 17:45:00` (the restart rule; that slot has its row). |
| 3.6b | deployed | bb4baa9, 2fe282b, 73ce936, 9744231, ae0de96, fd7689a | 2026-09-11 | Spec `docs/specs/3.6b.md` (v2, approved on conditions; corrected during the build to what was approved: `generate_once` returns `row`, a blank `model` is null, the byte measure, the 96 h ceiling row, the publish hook, DEBUG drops). **Built:**
- `ai_agent_client.py`: the brief contract (decisions entry A), typed errors, no retries, a 180 s bound
- `db.py` brief helpers; `GET /macro/brief` and `/health`'s `lastBriefAt` / `lastBriefTrigger` / `lastBriefError`
- `macro_brief.generate_once` + `POST /macro/brief/generate`; POST's 201 and GET share one serializer
- the news window since the latest XNYS close, 24–96 h
- the slot loop: 07:30 / 12:30 / 16:30 ET, ≤ 60 s `wallclock` chunks, 30 min grace, no catch-up; a restart skips a slot that has its row
- the regime trigger: `scheduler.on_check_published`, set by the lifespan only with the flag on; a size-1 queue; the 30 / 60 min debounce read from Postgres

**Verified:** risk-shield 562 → 629 in the twin. The four new SQL statements `PREPARE`d on `tradingfirm_dev` with the expected types (0 rows, nothing executed). Importing `main` builds no calendar (0 `get_calendar` calls). **Corrections during the build:** the GET handler named `macro_brief` shadowed commit 3's module import (17 failures; renamed `get_macro_brief`); an edit dropped a space in a `scheduler.py` comment (caught by numstat). **Estimate** (the first part on entry B's bands; counts are numstat added lines): code 718 vs 484–704 (fresh 440, ×1.63), tests 1,138 vs 846–1,222 (×1.21), mock 69 vs 100–150. Code tops re-cut in commits 1 (a new module, ×1.97), 3 (a new module, top ×2.3 by call), 4 (18 existing lines gaining a parameter: changed lines count as added) and 6 (the hook seam); commit 3's tests re-cut to 325 (shared helpers), and commits 5 and 6 did not land under their test estimates. **Twin round trip (decision 8), 2026-09-11 21:03–21:06 UTC:** the twin as pinned (flag off, no keys, `.invalid`). 20 fixture news items into `:8011` and one synthetic scored settle row; `generate_once(manual)` in the twin with ai-agent on a `MockTransport` → `generated` (1 mocked request, its inputs identical to the stored document, 0 FRED calls); `regime_change` → `debounced`; `:8013/macro/brief` served the fixture brief (`jsonb_typeof(brief)` object, inputs 1,465 bytes, `news.hours` 26, news `empty` because the fixture is 28–34 h old); the twin's POST → 503 `macro brief disabled`. Cleanup in one transaction deleted 1 / 1 / 20 rows: counts back to 0 / 0 / 0, Redis DB 1 `tf:risk:*` 0 before and after. **G15 done on go 2026-09-11**, after the 20:20 UTC settle (landed on `1cfb238d7a7b`):
- **Before:** `rollback-3.6b-base` = `1cfb238d7a7b` (the tag names the part it protects); logs saved to `logs/tf-risk-shield-pre-3.6b-20260911T210659Z.log` (0 ERROR); `newsPollStale: false`; `docker compose build risk-shield` → `d207a73dc310`.
- **Deploy:** `up -d --no-build` at 21:07:32 UTC, `/health` 200 at 21:07:35; one uvicorn; `Macro brief generation disabled` and both loops running; 0 ERROR.
- **After:** `/health` `macroBriefEnabled: false`, `lastBriefAt` / `lastBriefTrigger` / `lastBriefError` null; `GET /macro/brief` 404 `no macro brief yet`; `POST` 503 `macro brief disabled`; `/macro/brief/inputs` `news.hours` 26, news `ok` (43); `risk.macro_briefs` 0 rows; the 21:15 poll `100 fetched, 100 sent`, `newsPollStale: false` before and after; post-deploy logs saved beside the pre-deploy ones (`logs/tf-risk-shield-post-3.6b-20260911T211523Z.log`, 0 ERROR, 0 WARNING). No slot in the window; the next is Monday 09:30 ET. |
| 3.4b | done | 177ffc6, 8c5648b, ca4b72c, 20a6fed, 4eefa63, 2e7e48b, 770892f | 2026-09-14 | Spec `docs/specs/3.4b.md` (v1 for step 0 at gate 1, v2 approved 2026-09-14). **Step 0:** R1 / R2 unattended (Sun 19:45 / 20:15 ET) showed the 1d futures bar updating live in the Globex evening session, within 0.013 % of the 1h close, labelled with the ET date the session started and not flipping at UTC midnight → the night download is `1d` / `5d`. R3 (Tue 08:30 ET) answers Q4 (`^VIX` pre-market). **Built:** night slots at :15 / :45 ET while CME trades and no XNYS session runs (155 a week; the 17:00–18:00 halt, the Friday close and the 45 min before an open cut by hand, since `CMES` models none of them); `scoring/overlay.py` (worse of ES=F / NQ=F since the settle → −1.5 / −3 / −5 % caps at 69 / 39 / 19, `min(base, cap)`); `run_night_check` copying the settle's monitors and never downloading what it cannot use; the same cap on market checks, the settle exempt and storing the reference; `futures` + `overlay` on every row, the payload (append-only) and `/market/*`; `health.overlay` in the brief's inputs. **Verified:** 629 → 691 in the twin, 45 authored / 62 collected new tests, 12 updated in place. Twin round trip: a synthetic settle plus a patched −3.2 % view gave `night` 39 DANGER, trend declining, overlay applied, published on `tf:risk:dev:health` only; all three endpoints served it; cleanup 2 → 0 rows and Redis DB 1 `tf:risk:*` 1 → 0. **Estimate:** code 686 vs 555–829, tests 754 vs 820–1,184 (under the floor). Commit 5 measured 219 code against 110–160 and was split into 5a / 5b before it existed; commits 2, 3, 4 and 6 landed under their test floors. **G15:** waiting for a go. |
| 3.4c | built, deploy waiting | 50eeffb, 1b3e865, bee8c62, 7b47849, 69b68fa, 007f315, 6d7a42e | 2026-09-14 | Spec `docs/specs/3.4c.md` (v2, approved 2026-09-14 with four changes: staleness stated per input, the regime reason on the **capped** score, the log graded on `levelAtClose`, and NaN sanitised before publish). **Built:** `scoring/weekend.py` (reasons, weights, LOW/ELEVATED/HIGH with the active-situation override, phrase regexes, the VIX snapshot, the non-finite guard); the econ calendar's fourth type `event` with the duplicate rule moved to (date, time, type); `weekend_inputs.py` (calendar window close→open, news behind a 300 s reuse, the situation flag) and `PUT`/`DELETE /market/weekend/situation` behind `WEEKEND_WRITE_TOKEN`; the gate on the live path (weekend-eve session = last session before a ≥ 2-day gap, from close − 30 min, 8 rows, night rows excluded); `weekend` on the row, the payload (append-only) and both `/market/*` routes, plus `/health`'s three keys; `weekend_log.py` + `GET /market/weekend/log` with the per-level summary. **Verified:** 691 → 857 in the twin, 7 tests updated in place (an additive key breaking exact-dict assertions, plus the calendar's duplicate case). Off-window proof: two checks (a Wednesday, and Friday 15:25) byte-identical to `bee8c62` across indicators, payload, check_status and logs (sha256 `1c9b2d63…`). **Estimate:** rows 1, 2, 3a and 3b re-cut or re-banded and recorded in D12 with a reason each; row 4 (226 code / 240 tests) landed inside 215–325 / 207–299 after 3b and 4 were re-estimated before their code was written. **Twin round trip 2026-09-14 19:55–20:00 UTC:** three synthetic Friday rows (2026-09-11 15:45 LOW / 15:55 HIGH / 16:20 settle ELEVATED) plus a Monday open row in `tradingfirm_dev`; `:8013/market/health` served each level as it landed, `/market/indicators` the Monday row with `weekend: null`, and `/market/weekend/log?weeks=1` one scored weekend — `levelAtClose` HIGH, `levelAtSettle` ELEVATED, ES −2.0 %, NQ −3.0 %, next open 44 DANGER, `disagreements: 1`. The write path ran end to end on a token-enabled clone of the twin image (port 8014, same `tradingfirm_dev` / Redis DB 1 / dev channel): PUT 200 with a 172 800 s TTL, wrong token 401, DELETE 200 `cleared: true` then `false`; the real twin answered 503 `no WEEKEND_WRITE_TOKEN`. Cleanup in one transaction: 4 rows deleted, `health_checks` / `macro_briefs` back to 0, Redis DB 1 `tf:risk:*` 0, clone removed. Prod untouched throughout (175 rows, 0 with a `weekend` key). **G15:** waiting for a go; `WEEKEND_WRITE_TOKEN` must be in `.env` before the prod build. |
| CI | done, closed | 610cd67, 0b2c067, a01ca61 | 2026-09-17 | Spec `docs/specs/ci-coverage.md` (approved 2026-09-17 with five changes). `.github/workflows/tests.yml`: push + PR, dev-stage image per service, `--network none`, read-only source + `/migrations` mounts, twin non-secret env, no DB/Redis (every test fakes them); fails on any failed or skipped test. `pytest-cov==7.1.0` in both `requirements-dev.txt`, `.coveragerc` omits `tests/`, no threshold. CLAUDE.md data-engine list was stale (5 of 17 files); the workflow is now the reference list. **Fix first:** 3 dossier tests failed on main from the real clock, frozen in `app_state`. **Verified in the twins:** data-engine 452 passed, 83 % (2912 stmts); risk-shield 857 passed, 97 % (3095 stmts); ~10 s each. **CI run [#1](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35200317643) on `0b2c067`, green (job log):** data-engine 452 collected, 452 passed, 0 skipped, TOTAL 83 % (2912 / 487 missed), pytest 18.83 s, suite wall-clock 20 s; risk-shield 857 collected, 857 passed, 0 skipped, TOTAL 97 % (3095 / 107 missed), pytest 17.10 s, suite wall-clock 19 s. Follow-up (docs commit): the 4 summary lines also go to a notice annotation (logs need a login), `actions/checkout@v5`. **Closed with run [#3](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35202605733) on `a01ca61`, green:** both notices show on the run page without a login (data-engine: collected 453, 453 passed, TOTAL 83 %, wall-clock 20 s; risk-shield: collected 857, 857 passed, TOTAL 97 %, wall-clock 13 s), so job numbers no longer need pasting; no Node 20 warning. **Deferred, since fixed by dossier-clock:** 2 dossier tests break from ~2026-10-01 (`assemble.py` ignores `ctx.now` for filings/news/calendar), see the spec's clock audit. |
| dossier-clock | done | f75f16a | 2026-09-17 | Spec `docs/specs/dossier-clock.md` (approved 2026-09-17). `dossier/assemble.py` passes `today=ctx.now_utc().date()` to `company_news`, `earnings_calendar` and `recent_filings`, so an injected `ctx.now` reaches every dossier date window. Prod unchanged: `main.py` never sets `now`, so the argument is the same `datetime.now(timezone.utc).date()` the fetchers fell back to, and cache keys carry no date. New `test_assemble_passes_ctx_today_to_fetchers` (fails without the fix). **Verified:** twin 453 passed, 0 skipped, 83 %; `caps_applied_and_flagged` and `filings_block_flag_flags_truncated` pass under time travel at 2026-09-17, 2026-10-01, 2027-01-01, 2027-04-01, 2027-07-01 and 2027-09-30 (full suite 453 passed at each). |
| 3.4c.1 | closed, not built | — | 2026-09-21 | Would have handled the recurring ~1 % ES/NQ step at the 00:15 ET night slot. Skipped: the step was present on all four nights before the Sep expiry (09-14 → 09-18) and absent on the first night after (09-20 → 21: ΔES −0.003, ΔNQ −0.022). Readings and the ruling are in `docs/decisions.md` 2026-09-21. The real fix is contract-aware futures, due before the December expiry. |
| 4.1 | built, no deploy needed | 9d8b635 (pre-existing entry, alone), 4d5db6b, d99ecfc, 24f64e9, + this row's own `docs:` commit | 2026-09-20 | Spec `docs/specs/4.1.md` (v2 — the first draft was the Anthropic SDK; rewritten for OpenRouter and approved 2026-09-20 with fixes A–M, all folded into the spec before commit 3). **Direction change:** one OpenAI-compatible client with a configurable `base_url`, not the Anthropic SDK — `docs/decisions.md` 2026-09-20 supersedes D16. **Built:** `config.py` (`llm_base_url`, `llm_api_key` as `OPENROUTER_API_KEY`, Sonnet 5 as both model defaults, cap 100, 8000 tokens, 150 s, 900 s cooldown, optional attribution headers); `cache.py` (the `tf:ai:` namespace, `et_day`, `day_counter_key`, `INCR` + `EXPIRE 129600 NX`, reserve-then-call with the over-cap branch the only release, cooldowns that store the status that caused them, `MemoryCap` / `MemoryCooldowns`); `providers/base.py` (the ABC, `LLMResult`, nine typed errors, step-1 validation of system/user/label/schema); `providers/openai_compat_provider.py` (`MODEL_REASONING` read from OpenRouter's own models API, `resolve_effort`, the lazily-built `AsyncOpenAI`, the status→error map, the response walk, one INFO line per call with `cost` and one WARNING per raised error); three-stage Dockerfile, `requirements-dev.txt`, `pytest.ini`, `.coveragerc`; `ai-agent-dev` on 8014. **Verified:** ai-agent 0 → 84 tests, green in the twin and under `docker run --network none`, 93 % coverage (99 % on the provider, 100 % on config). risk-shield 857 → 857 and data-engine 453 → 453, untouched. G4 probe in the twin on the final dev pins: `openai 3.16.2` matching the pin, `pip check` clean, `respx installed: False`, offline construction returning `max_retries=0` / `timeout=150.0`. `docker-compose.yml` insertion-only: all 9 pre-existing service blocks SHA-256-identical. **Verified against OpenRouter's docs/API, not memory:** structured-output support for all three model ids; the `reasoning` object per model (GLM-5.3-Flash mandatory, efforts `max/high/low`; Haiku 4.5 advertises none, so it gets no `reasoning` field); `usage: {"include": true}` deprecated and never sent; 200-with-an-`error`-body is a real non-streaming case (`choices` comes back `None`) and maps to `LLMUnavailable`; 408 is a documented timeout status. **Corrections during the build:** `validation_alias` alone made `Settings(llm_api_key=…)` silently do nothing (`AliasChoices` instead); `release_call` did not mirror `reserve_call`'s memory fallback, so a Redis outage left the in-process counter permanently high; two tests read the twin's own env (`SERVICE_NAME`, `LLM_BASE_URL`) instead of the declared defaults. **No live LLM call anywhere**, and none until the key is set and a go is given. **G15:** nothing waiting — no prod rebuild, no migration, no `.env` edit. **CI run [#5](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35521422342) on `b58050e`, green on all three jobs** (2026-09-20 16:02 UTC, push): data-engine 453 passed, 0 skipped, TOTAL 83 %; risk-shield 857 passed, 0 skipped, TOTAL 97 %; ai-agent 84 passed, 0 skipped, TOTAL 93 %. The `ai-agent` matrix entry and CLAUDE.md's third test command landed in `b58050e`; it inherits the shared run step, so it runs `--network none` like the other two. **Noted, not fixed:** data-engine and risk-shield each print one `DeprecationWarning` from `starlette/testclient.py:37` (`anyio.abc.BlockingPortal` alias, starlette 0.46.2 on anyio 4.15.x) — third-party to third-party, on the import of `TestClient`. ai-agent prints none because 4.1 has no routes and so no `TestClient`; expect it there from 4.2. **4.1 closed.** |
| 4.2 | deployed | 9f0eede, 839cf72, 4057871, b300bd1, bacd458, 0a8fd6b, 6dadd8a, c1fbafd | 2026-09-20 | Spec `docs/specs/4.2.md` (v2, approved 2026-09-20 with six changes: cached items with an id are written back too, one reservation rule released on every pre-wire refusal, the cap number and its arithmetic in the spec, both routes' exposure stated, the compose table reconciled to 10 blocks, and the cost keys kept 40 days not 36 h; then Redis persistence and the cross-source-duplicate limitation recorded, and a 500 for the `validate_request` branch). **Built:** data-engine `POST /news/{id}/sentiment` (replaces the column, never merges) plus `id` and `sentiment` on `GET /news/market`, no migration needed — 003's `sentiment JSONB` was already there; ai-agent's first lifespan (Redis via `cache.create_redis` + one `httpx` client, bounded by `STARTUP_TIMEOUT`, fail-open, **no DB pool**); `cache.headline_digest` (the one normalization, url-then-title), a keyword-only `name=` putting the classifier's counter on the global counter's code path, ET month keys, the cost totals and the classification cache; `classifier.py` (one call per batch of ≤30, unseen only, in-batch digest collapse, the item contract, whole-batch rejection); `prompts/headline_classify.md` + a loader that reads at first use, not import; `data_engine_client.py` (fail-open, never raises); `POST /classify/headlines` and `GET /usage`. **Verified:** ai-agent 84 → 236 tests (93 % → **99 %** coverage; `cache.py` 100 %, `data_engine_client.py` 100 %, so 4.1's uncovered `create_redis` now has both a caller and three direct tests), data-engine 453 → 490, risk-shield 857 → 857 untouched. **Twin round trip (real Postgres + real Redis DB 1, stubbed provider, no LLM):** two rows seeded into `tradingfirm_dev`, call 1 classified 2 and wrote both (`POST /news/85|86/sentiment` 200), call 2 returned `cached: 2, classified: 0, model: null` with the provider still on **1 call** and **`writtenBack: 2`** — decision 6b proven live — and `/news/market` showed both rows carrying their `sentiment` object. `GET /usage` answered from Redis. **Two twin locks bit during the build**, which is them working: 23 endpoint tests had been reading `LLM_CLASSIFIER_DAILY_CALL_CAP` and `DATA_ENGINE_URL` out of the process environment (`_env_file=None` does not disable it), and now pin the knobs they depend on. **Compose:** 10 service blocks, 2 changed (`ai-agent`, `ai-agent-dev`), the other 8 SHA-256-identical before and after. **Corrections during the build:** namespacing `MemoryCap` by counter name would have broken three 4.1 test assertions the spec promised were unchanged — one `MemoryCap` instance per counter instead; a prompt assertion straddled a hard wrap. **No live LLM call anywhere.** **G15 waiting for a go:** `docker compose build ai-agent` + `up -d ai-agent` (which is also what applies the loopback port and the new env), `up -d data-engine` for the sentiment route, and the `OPENROUTER_API_KEY` line in `.env` before the first live call (`tests/classify_live.py`, committed unrun, verified to exit 2 in the twin). **CI run [#7](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35589958807) on `c1fbafd`, green on all three jobs** (2026-09-21 10:40 UTC, push): data-engine 490 passed, 0 skipped, TOTAL 84 %; risk-shield 857 passed, 0 skipped, TOTAL 97 %; ai-agent 236 passed, 0 skipped, TOTAL 99 %. One `DeprecationWarning` per job, the anyio one, now in ai-agent as predicted. **4.2 closed.** **Deployed 2026-09-21 on the user's go**, build then `up -d`, each within a minute of a night slot writing and clear of the :00/:15/:30/:45 UTC news polls (no scheduled scan exists to avoid): `tf-ai-agent` started 12:15:24 UTC on `7eb3f76…`, between the 08:15 and 08:45 ET slots, both `within`, on `127.0.0.1:8004` with `llmConfigured: true`; `tf-data-engine` started 12:45:24 UTC on `b26282e…`, after the 08:45 slot, followed by a clean 13:00 poll (100 sent, ingest 200) and a clean 09:30 ET market row (70 HEALTHY). **First live calls**, run as `classify_live.py`'s contents through `python3 -c` because `tests/` is not in the prod image: one headline for $0.00394 (1520 in / 90 out), five for $0.007014 (1672 / 367), both `finish: stop` with the contract validated. That closes 4.1's open `strict: true` risk. The day's spend was $0.010954. Both calls showed `cacheRead: 0` because ai-agent sends no `cache_control`: spec 4.1 dropped it, and OpenRouter caches Anthropic models only when `cache_control` is sent, at the top level or per block. Sonnet 5's minimum cacheable prefix is 1,024 tokens (Anthropic's cache-limitations table). Caching is the first item for 4.4's spec. The six made-up live-check classifications were deleted from prod Redis afterwards; the counters and the spend were kept. |
| 4.3 | done, closed | d16aefb, 232151e, 3bbbe6c, d49b44d, + this row's own `docs:` commit | 2026-09-21 | Spec `docs/specs/4.3.md` (approved 2026-09-21 with six changes: Decimal from the inputs, a 25 % max-position bound, one check order, `no_target` at ATH as an open gap, no size estimate, the CI edit as its own `chore:` commit). **Built:** `models/verdict.py` and `grading/plan_math.py`, both pure (an `ast` import test pins it). No route, no LLM call, no Redis/Postgres, no compose change. **Verified in the twins:** ai-agent 236 → 346 (+46 schema, +64 plan math), 99 %, both new modules 100 %; data-engine 490, risk-shield 857 unchanged; worked example A printed from the twin. **G15:** nothing waiting; the code ships with 4.4's rebuild. **CI run [#12](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35622615963) on `d49b44d`, green on all three jobs** (2026-09-21 15:59 UTC, push): data-engine 490 passed, 84 %; risk-shield 857 passed, 97 %; ai-agent 346 passed, 99 %; 0 skipped. **4.3 closed.** |
| 4.4 | deployed, closed | 222d552, f5d07a3, 497448d, 9f44be8, 108fa4f, ac4459f, c5492a6, 777e79e, 904269f, aca52c7, 2d3ebd4, 8364f55, 1aa272a, 73cac89, a0db9ea, 1d3ac5d, 2ffc860 (docs), a6e36a8 (workflow), 5c59bb0, f79608c, ded1570 (docs), a9e4aff, a401310, 3065c8b (docs), 0b640b8, d82c16e (fix), af059fb (docs), + this row's own `docs:` commit | 2026-09-21 | Spec `docs/specs/4.4.md` (approved 2026-09-21 with nine changes: NULL account seed, ledger = wire calls only with `served_count` on the verdict, in-process classifier, the seed on 4.1's keys by its expiry rule, `entry=auto` = last close in cents, OpenRouter's dated list price, no deploy-window wording, the unstored verdict logged whole, the ATR price bucket). **Built:** data-engine's dossier news carries `id` + stored `sentiment`, and the sentiment contract an optional `eventKey`; the classifier's required `eventKey` + `knownEventKeys`; migration **007** (005/006 were taken); ai-agent's pool, `ai.llm_calls` and the startup cap seed; `events.py`, `upstream.py`, `analyze.py` (pure), `analyst.py`, `prompts/verdict.md`; `POST /analyze/{ticker}`; the provider's `cache_system`, off. **Verified in the twins:** data-engine 490 → 504 (84 %), ai-agent 346 → 595 (99 %; `analyst.py` and `analyze.py` 100 %), risk-shield 857 unchanged, 0 skipped; ai-agent also green under CI's exact `--network none` read-only run. Both prod stages built under `:verify` tags; `prompts/verdict.md` ships, `tests/` does not. 007 applied to `tradingfirm_dev`, rerun a no-op, the file re-executed directly. **Twin round trip (real `tradingfirm_dev`, Redis DB 1, data-engine-dev and risk-shield-dev over HTTP, stubbed provider):** call 1 classified 3 headlines, wrote all 3 back through `POST /news/{id}/sentiment` with their `eventKey`, grouped them into **1 event**, stored a verdict + 2 ledger rows; call 2 `cached: true`, provider still at 2 calls, `served_count` 1; a fourth, high-relevance headline → call 3 sent **only that one** and paid for a new verdict. risk-shield-dev had no health row, so every call ran with `macro: unavailable` (fail-open, live). The twin's classifier-cap lock refused run 1, which is it working. **Cap seed:** Redis DB 1 counters deleted, twin restarted → `llm_calls` 4, `classifier_calls` 2, `cost_day` 0.0496 back from the ledger, TTLs 129 594 / 3 455 994. Over HTTP the twin answers 503 `no API key`, 422, 404, and 409 `settings missing` after cleanup. Cleanup in one transaction: 4 / 2 / 4 rows deleted, `account_size` back to NULL, Redis DB 1 `tf:ai:*` 0. Prod untouched: no `ai.llm_calls`, `schema_migrations` 6. **Compose:** 10 blocks, 2 changed, 8 SHA-256-identical. **Corrections during the build** (in the spec): `LLMAuthFailed` is a wire call; a rejected batch's cost is now counted; over-long answer strings are trimmed; three commits over 600 lines split before any push. **Added before the first push:** `LLM_PROVIDER_ORDER` (`5c59bb0`; compose line `f79608c`, 9 of 10 blocks identical, `ai-agent-dev` among them), and the verdict string trim named in the spec's must-not as its one exception. ai-agent 595 → 604. **Changed before the push (`a9e4aff`, compose `a401310`):** the order defaults to `anthropic` with `allow_fallbacks` always true, the false path removed; the serving host is stored in `ai.llm_calls.host` (007 edited in place, rebuilt on `tradingfirm_dev`; a real 17-column insert read back `host = Google Vertex`, then deleted) and a fallback logs a WARNING; the twin test for the setting is dropped, since the key, the caps and the `.invalid` URL are what protect the twin. ai-agent 604 → 617. **Then, in its own commit:** the explicit-empty opt-out removed — the `provider` field is on every request, and an empty or malformed order refuses to start. ai-agent 617 → 622. **Deployed 2026-09-21, each step on its own go:**
- **007 on prod:** `schema_migrations` 6 → 7, rerun a no-op, `account_size` NULL until set by hand.
- **data-engine** `b26282ebbebf` → `847679939020` at 18:45:02 UTC, in the minute after the 18:45 news poll landed; the 19:00 poll landed on the new image (`_MARKET` 491 → 492 → 493), health rows 18:40–19:00 one per slot. One AAPL dossier: 200 in 9.97 s, 30 headlines, each with an `id`. `rollback-4.4-base` tags on both services.
- **ai-agent** `7eb3f76c428f` → `1e5770a7bf30` at 19:04:55 (no scheduled writer), recreated 19:11:51 for `LLM_VERDICT_CACHE=true`: `db_connected`, `capsSeeded`, caps 100 / 40, provider order `anthropic` with fallbacks on. The seed left this morning's 2 calls and $0.010954 untouched (empty ledger, max of the two).
- **Live check, AAPL twice (the second `fresh=true`, 49 s later):** `avoid` 78 / 75, entry 338.95 `last_close`, CAUTIOUS, plan rejected `low_r` (best R 0.19) and passed through; 30 headlines → 25 keys → 15 events, 30 written back. Ledger: classify 6,349 in / 2,628 out **$0.038978**; verdict 6,228 / 541, `cacheWrite` **2,237**, **$0.018985**; verdict 6,239 / 578, `cacheRead` **2,237**, **$0.014231**. Host `Anthropic` on all three, no fallback WARNING. **$0.072194**, equal to `/usage`'s rise. `LLM_VERDICT_CACHE=true` stays. The classifier estimate ($0.03) was low; corrected in the spec, CLAUDE.md and the script.
- **Found by the live call, fixed in `d82c16e`:** the verdict's ledger row was stamped at request start and sorted before its classifier row; the model added its own no-plan flag beside the code's. ai-agent 622 → 629. Its prod rebuild ships with 4.7's. **CI run [#15](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35647179308) on `af059fb`, green on all three jobs** (2026-09-21 19:49 UTC, push): data-engine 504 passed, 84 %; risk-shield 857 passed, 97 %; ai-agent 629 passed, 99 %; 0 skipped. Run [#14](https://github.com/Zubair-Iqbal-1/TradingFirm/actions/runs/35637862222) on `0b640b8` was green before the deploy. **4.4 closed.** |
| 4.5 | built, deploy waiting | a9f5f6c, c29b4eb, 3ff7ab5, ecb0f2f, 9ba8027, 9d7646e, e468192, 1c69107, 0941b1c, 1fd70e2, 5061961, + this row's own `docs:` commit | 2026-09-21 | Spec `docs/specs/4.5.md` (v2, approved 2026-09-21 with three changes: a Redis set of blanked tickers that run last, a soft hourly presence check, F2 recorded as a data-engine defect; addition 2026-09-22: horizons +30 and +60). **Built:** migration **008** (`session_date`, `first_hit`, `r_multiple`, `ask_session_bars`, and 007's horizon CHECK widened to 1 / 5 / 20 / 30 / 60; 007 frozen by hash); `journal/sessions.py` (XNYS via `exchange_calendars` 4.13.2, lazy, bounded 2026-01-01 → today + 1 y); `journal/scoring.py` (pure, Decimal); `journal/runner.py` (the 17:30 ET slot, data-engine refresh per due ticker, ≤ 20 attempts 5 s apart, two-blank rule, blanked set, 18:10 deadline, one transaction per ticker); `journal/stats.py` + `GET /journal/stats` (per model); `wallclock.py` copied from risk-shield; `JOURNAL_SCORING_ENABLED` on in prod compose, hard-coded off in the twin. No LLM path. **Facts found:** a refresh cooldown proves a completed refresh, not a bar; a delisted or rate-limited refresh answers 200 with zero bars (F2); stored bars are split/dividend-adjusted; a daily bar is stored at midnight UTC and an hourly one at its start. **Verified:** ai-agent 629 → 777 (99 %), also under CI's exact `--network none` read-only run; data-engine 504 → 505; risk-shield 857; 0 skipped. Twin round trip on `tradingfirm_dev` against `data-engine-dev`: AAPL asked in session with a plan, MSFT after the close without → 5 + 5 rows across four frozen slots, every number re-derived from the fixture outside the scorer with trading days counted by the fixture's own bar index (the first round, 3 + 3 before the addition, also showed a cooldown requeued once then skipped: F1 live); a rerun 0 refreshes 0 inserts; `/journal/stats` on :8014; `ai.llm_calls` 0; cleaned up. Prod stage under `:verify`: **420.6 → 556.1 MB** (pandas + numpy), no calendar at import, no `tests/`. 008 re-applied on `tradingfirm_dev` and on a scratch copy of prod's 007 table (CHECK widened, 10 refused, rerun exit 0). **Split at commit time:** `score_once` measured 863 lines → feat (565) + test (298). |
