# Housekeeping

The list of agreed cleanup items, as of 2026-09-22. **This file is the list** (it replaces the copy that lived in session memory). An item is added only when Zubair agrees to it, nothing here starts without a go, and every item that touches prod compose or a prod service is a G15 change. A done item is struck through with its commit, not deleted.

| # | what | where | done when |
|---|---|---|---|
| 1 | Redis durable persistence: named volume + AOF | `docker-compose.yml` `redis` block (`redisdata:/data`, `--appendonly yes`); why in `docs/decisions.md` 2026-09-20 | Its own spec and go; a `docker compose down` + up keeps ai-agent's daily counters; SHA-256 proof that the other nine compose blocks are unchanged |
| 2 | The nine remaining published ports to `127.0.0.1` (postgres 5432, redis 6379, data-engine 8001, data-engine-dev 8011, signal-engine 8002, risk-shield 8003, risk-shield-dev 8013, ai-agent-dev 8014, web 3000) | `docker-compose.yml` `ports:` lines; ai-agent moved in 4.2 | Every port binds 127.0.0.1; per-block SHA-256 before/after table; prod recreates on a go, risk-shield's out of XNYS hours; item 11 ships in the same batch |
| 3 | Pin the CI runner to `ubuntu-24.04` | `.github/workflows/tests.yml` `runs-on:` (the Ubuntu 26 notice is on every run since #7, latest #17) | Committed and pushed by Zubair **before 2026-10-19**; the next run shows no migration notice |
| 4 | Pin `anyio`: it floats as a transitive dependency and already differs (4.15.0 in data-engine, 4.15.1 in risk-shield and ai-agent); starlette's TestClient warns on it in every suite | `services/*/requirements.txt` (no line today) | One version pinned in all three services, twins rebuilt, CI green with the pin; the DeprecationWarning gone or recorded as accepted |
| 5 | Commit risk-shield's roll canary | `services/risk-shield/tests/roll_live.py` (untracked); CLAUDE.md live-script list | The script is committed and named in CLAUDE.md's live-script list in the same commit. Nothing else: its log never goes into the repo |
| 6 | "Premarket scan window" names a window with no writer: data-engine schedules no scan (only a manual `POST /scan/run`) | `CLAUDE.md:106`, `docs/decisions.md:522`, `docs/specs/3.6a.md:352` | CLAUDE.md states what a data-engine rebuild must actually avoid; a new decisions entry supersedes 2026-09-10's line (decisions are append-only); the 3.6a spec stays as the record |
| 7 | data-engine refresh answers `200` with 0 bars and starts the 15 min cooldown when the download is empty — delisted or rate-limited alike (yfinance 1.5.1 swallows rate limits); `/dossier` is fooled too | `services/data-engine/main.py` `refresh_ticker_bars` (`:521–574`); spec 4.5 F2; decisions 2026-09-21 | Fixed with option C (a nightly bar job in data-engine), not alone: an empty download is not a 200 and starts no cooldown, a rate limit is surfaced; 4.5's two-blank rule then re-checked |
| 8 | (a) Scanner RVOL treats the last daily bar as today's partial without checking its date, on a weekday + hours clock, and scales by 390 min | `services/data-engine/scanners/market_scanner.py:381-388` | Last bar counts as today's only if `ts.date()` equals today's ET session from `exchange_calendars`; scale by the session's real length (210 min on an early close). No action while scans are manual; **raised at 6.3's G1** so the 5-minute bar builder does not inherit it |
| 9 | (b) Daily bars are midnight UTC only because yfinance 1.5.1 returns a naive daily index; an aware one would write a second row per date | `services/data-engine/db.py:546` (`bar_records_from_df`) | At the next yfinance pin bump: the index is still naive / midnight UTC, or the write path normalises before upsert; a test asserts the stored ts shape |
| 10 | (c) `aggregate_4h` blocks by `index.hour // 4` in the index's own tz; correct only because its one caller passes an ET frame | `services/data-engine/indicators/moving_averages.py:41-42`; caller `scanners/market_scanner.py:440` | When a second caller appears (not before): an explicit tz argument or an assert that the index is ET |
| 11 | (d) `GET /stock/{t}/bars` reads a naive `since` as UTC | `services/data-engine/main.py:610-614` | A naive `since` answers 422 with a test; shipped in the same batch as item 2 |
| 12 | Two doc fixes from Zubair's 2026-09-20 review (not in the repo yet): (i) the weekend block's news cost is written as about two `/news/market` calls for eight rows — it is five; (ii) the `Missed 1 health check slot(s)` WARNING logged after a restart is a false positive, but the docs record it as a missed slot | (i) `CLAUDE.md:125`, `docs/specs/3.4c.md:130` (D6, "~2 fetches a Friday"); (ii) `docs/runbook.md:156`, `docs/decisions.md:586` (2026-09-10 "A risk-shield restart skips the slot it lands on") | Docs only, in the same batch as item 6: CLAUDE.md says five calls; a new decisions entry corrects both (the 3.4c spec and the 2026-09-10 entry stay as records); the runbook row says the warning was a false positive |
| 13 | `POST /stock/{t}/refresh` runs the earnings lookup and the Alpha Vantage fallback for every ticker, ETFs included: the 2026-09-22 benchmark refresh (SPY + five sector ETFs) logged six "No earnings dates found" and spent up to 6 of AV's 25 daily calls for nothing | `services/data-engine/main.py` `refresh_ticker_bars`; `providers/context/earnings.py` | Skip the lookup for a known benchmark / ETF ticker (the `SECTOR_ETFS` values + SPY), or a `?skipEarnings` flag; a test proves an ETF refresh makes no earnings call |
| 14 | `journalLastRunAt` / the last slot result live only in the ai-agent process, so a deploy erases the slot proof (2026-09-25: the 09-24 slot could not be proven after go 4). Persist them in Redis or the DB | `services/ai-agent/journal/runner.py`, the `/health` fields | `/health` shows the last slot after a container recreate; one test |

Not items: dossier staleness ignoring holidays stays as documented (`staleWeekdays`, `dossier/assemble.py:165`).
