# Discovery is restricted the same way risk-shield's is (Part 3.1): only
# tests/test_*.py is collected, so a bare `pytest` can never pick up a
# live-API script added later under tests/ (4.8's live probe).
[pytest]
testpaths = tests
python_files = test_*.py
