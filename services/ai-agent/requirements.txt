fastapi==0.115.12
uvicorn[standard]==0.34.3
httpx==0.28.1
redis==5.3.0
asyncpg==0.30.0
pydantic==2.11.4
pydantic-settings==2.9.1
# Part 4.1: the one LLM client, OpenAI-compatible, pointed at OpenRouter
# through config.llm_base_url. It pulls in httpx2 (a DIFFERENT distribution
# from the httpx pinned above — both install side by side); that is the
# transport the provider's tests mock, so httpx2 is deliberately not pinned
# here: it is openai's dependency, not ours.
openai==3.16.2
# Part 4.5: XNYS sessions, holidays and early closes for the journal scorer.
# The same three pins as services/risk-shield/requirements.txt, where
# exchange_calendars 4.13.2 was probed (Part 3.4); pandas and numpy arrive
# with it and are pinned so both services resolve the same set. The calendar
# is built lazily with explicit bounds and kept as plain dates (G8).
numpy==2.3.0
pandas==2.3.0
exchange_calendars==4.13.2
